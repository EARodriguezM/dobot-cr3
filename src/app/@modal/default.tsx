// Nothing is being shown over the page. Every route needs this slot resolved,
// and for all but the intercepted ones the answer is "no overlay".
export default function NoModal() {
  return null;
}
